// const name = "Mica";
// const age = 18;
// const hasDriverLicense = true;

// if (name == "Mica" && age >= 18 && hasDriverLicense) {
//     console(`{name} can drive a car`)
// }
// else{
//      console(`{name} cannot drive a car`)
// }

// if (name == "Bob" && age >= 18 && hasDriverLicense) {
//     console(`{name} can drive a car`)
// }
// else{
//      console(`{name} cannot drive a car`)
// }

const name = "Mica";
const age = 18;
const hasDriverLicense = true;

if (name == "Mica" && age >= 18 && hasDriverLicense) {
    console.log(`${name} can drive a car`)
}
else{
     console.log(`${name} cannot drive a car`)
}

if (name == "Bob" && age >= 18 && hasDriverLicense) {
    console.log(`${name} can drive a car`)
}
else{
     console.log(`${name} cannot drive a car`)
}